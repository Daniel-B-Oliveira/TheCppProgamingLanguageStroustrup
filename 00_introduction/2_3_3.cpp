int main(){
    // char v[10];     // arrays of 10 characters
    // char* p;        // pointer to character

    // p = &v[3];      // p points to v's fourth element

    void another_function();
    {
        int v1 [10];
        int v2 [10];
        // ...
        for (int i=0; i<10; ++i) v1[i] = v2[i];
    }
}

